
 

#The assert checks if 1 + 1 equals 2.
#If it does, the test passes and nothing happens.
#If it does not equal 2, the test fails and shows the message "failed to add correctly".
def test_sample_function():
    assert 1+1 == 2, "failed to add correctly"
    
